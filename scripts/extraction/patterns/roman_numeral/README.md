# roman_numeral

## 厳密に言えること

- これは実質的に標準ローマ数字変換。
- 学習データで観測される整数の範囲は 1 から 100。
- 1 問ごとに例題は 3, 4, 5 個。
- 出力文字は `IVXLCDM` のみ。

## 実装上のパターン

- そのまま通常のローマ数字変換でよい。
- 必要なトークンは少なく、実質この集合で足りる。
  - `I, IV, V, IX`
  - `X, XL, L, XC`
  - `C`

## solver 用メモ

- few-shot に頼る必要はほぼない。
- ルールベース実装で十分。
